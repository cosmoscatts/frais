/**
 * 定义一些布局参数值（单位是 px）
 */
export const appLayoutParams = {
  /** 导航栏高度 */
  navHeight: 50,
  /** 多页签高度 */
  tabHeight: 40,
  /** 侧边栏宽度 */
  sideWidth: 240,
  /** 侧边栏折叠后的宽度 */
  sideCollapsedWidth: 64,
  /** 底部高度 */
  footHeight: 50,
  /** backTop 距离页面右部的高度 */
  backTopRight: 50,
  /** backTop 距离页面底部的高度 */
  backTopBottom: 200,
}

