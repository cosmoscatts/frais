/**
 * whether cache the app settings
 *
 * @default true
 */
export const cacheSettings = true

export default {
  /**
   * theme mode - 'dark' | 'light' | 'system'
   *
   * @default 'dark'
   */
  themeMode: 'dark',
}
